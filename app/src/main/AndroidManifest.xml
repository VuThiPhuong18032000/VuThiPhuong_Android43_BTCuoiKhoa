<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.vuthiphuong_android43_btcuoikhoa">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission
        android:name="android.permission.MANAGE_DOCUMENTS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.ACCESS_DOWNLOAD_MANAGER" />
    <uses-permission android:name="android.permission.ACCESS_ALL_DOWNLOADS" />
    <uses-permission android:name="android.permission.ACCESS_DOWNLOAD_MANAGER_ADVANCED" />

    <application
        android:name=".login.App"
        android:allowBackup="true"
        android:hardwareAccelerated="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VuThiPhuong_Android43_BTCuoiKhoa"
        android:usesCleartextTraffic="true">
        <activity android:name=".view.TrungTamTroGiupActivity"></activity>
        <activity android:name=".view.DonHangMuaActivity" />
        <activity android:name=".view.DonMuaChiTietActivity" />
        <activity android:name=".view.DonDangGiaoChiTietMainActivity" />
        <activity android:name=".view.DonDangGiaoActivity" />
        <activity android:name=".view.DonMuaActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".view.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".view.DiaChiNhanHangActivity" />
        <activity android:name=".view.ThanhToanActivity" />
        <activity android:name=".view.ThongTinCaNhanActivity" />
        <activity android:name=".view.TimKiemActivity" />
        <activity android:name=".login.SignupActivity" />
        <activity android:name=".view.GioHangActivity" />
        <activity android:name=".view.QuanAoSoSinhActivity" />
        <activity android:name=".view.QuanAoBeGaiActivity" />
        <activity android:name=".view.QuanAoBeTraiActivity" />
        <activity android:name=".view.SanPhamBanChayActivity" />
        <activity android:name=".view.SanPhamNoiBatActivity" />
        <activity android:name=".view.ChiTietSanPhamActivity" /> <!-- <activity android:name=".fragment.ThemVaoGioHangFragment" /> -->
        <activity android:name=".login.LoginActivity">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>